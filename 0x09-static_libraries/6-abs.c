#include "main.h"

/**
* _abs - gets the absolute value of a number
*
* @i: input interger
*
* Return: absolute value
*/
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
