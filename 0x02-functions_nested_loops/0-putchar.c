#include "main.h"

/**
 * main prints _putchar
 *
 * Return is zero
 */
int main(void)
{
	char put[] = {_, p, u, t, c, h, a, r};
	int count, size;

	size = sizeof(put) / sizeof(int);
	for (count = 0; count < size; count++)
	{
		_putchar(str[count]);
	}
	_putchar('\n');
	return (0);
}
